//Notes für alles mögliche\\


List<String> t = new ArrayList<>();

Zustandsbasiertes Testen - Zustandsablauf festlegen

UML Aktivitätsdiagramm - glasklar


Single Design Pattern

Konstruktor private + Klassenvariable + getInstance Methode + return INSTANCE //Singleton
Persistent Speichern = nach dem Runterfahren immernoch da: Object Output Stream (oos) .ser + writeObject
ObjectInputStream + FileInputStream
Serializable
geekforgeeks

Übung4:
2) Tool welches User Stories(Anforderungsbeschreibung) verwalten kann
    CLI mit coolen Interface
    dump mit Tablle
    sc.nextLine()
    System.in
    input.split(" "); -> gibt Array
    System.err.println -> Fehlermeldung
    liste.sort(comperator);
    eigenen comperator definieren
    filtern mir l.stream , siehe Musterlösung 2